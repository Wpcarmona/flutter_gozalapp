import 'package:flutter/material.dart';

class VacilonView extends StatelessWidget {
  const VacilonView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Vacilon View'),
    ));
  }
}