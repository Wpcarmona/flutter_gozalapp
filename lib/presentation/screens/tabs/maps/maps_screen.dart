import 'package:flutter/material.dart';

class MapsScreen extends StatelessWidget {

  static const name = 'maps-screen';
  const MapsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Maps Screen'),
      ),
    );
  }
}